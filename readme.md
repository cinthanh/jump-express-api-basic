# init project

```shell
    npm init -y
```
# install express
```shell
    npm install --save express
```
# install nodemon
```shell
    npm install --save-dev nodemon
```
# install nodemon
```shell
    npm install --save morgan
```